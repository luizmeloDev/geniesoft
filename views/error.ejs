<%- include('partials/header') %>

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <div class="card border-danger shadow-lg">
                <div class="card-header bg-danger text-white">
                    <h4 class="mb-0"><i class="bi bi-exclamation-triangle-fill me-2"></i>Ocorreu um Erro</h4>
                </div>
                <div class="card-body text-center">
                    <h5 class="card-title text-danger"><%= message || 'Algo deu errado' %></h5>
                    <p class="card-text"><%= (typeof error !== 'undefined' ? error.message : '') || 'Desculpe, um erro inesperado aconteceu.' %></p>
                    <div class="mt-4">
                        <a href="javascript:history.back()" class="btn btn-secondary me-2"><i class="bi bi-arrow-left"></i> Voltar</a>
                        <a href="/" class="btn btn-primary"><i class="bi bi-house"></i> Página Inicial</a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<%- include('partials/footer') %>
