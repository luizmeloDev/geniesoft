<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> - Portal do Admin</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/boxicons@2.0.7/css/boxicons.min.css" rel="stylesheet">
    <!-- Admin Mobile Optimizations -->
    <link href="/css/admin-mobile.css" rel="stylesheet">
    <style>
        .stat-card {
            transition: transform 0.2s;
        }
        .stat-card:hover {
            transform: translateY(-2px);
        }
        .stat-icon {
            width: 60px;
            height: 60px;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
            font-size: 1.5rem;
        }
        .stat-odp { background: linear-gradient(45deg, #667eea 0%, #764ba2 100%); }
        .stat-cable { background: linear-gradient(45deg, #f093fb 0%, #f5576c 100%); }
        .stat-customer { background: linear-gradient(45deg, #4facfe 0%, #00f2fe 100%); }
        .stat-network { background: linear-gradient(45deg, #43e97b 0%, #38f9d7 100%); }
        
        .recent-item {
            border-left: 4px solid #007bff;
            transition: all 0.2s;
        }
        .recent-item:hover {
            border-left-color: #0056b3;
            background-color: #f8f9fa;
        }
        
        .quick-action-card {
            transition: all 0.3s;
            cursor: pointer;
        }
        .quick-action-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 8px 25px rgba(0,0,0,0.15);
        }
    </style>
</head>
<body>
    <!-- Navigation -->
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
        <div class="container-fluid">
            <a class="navbar-brand" href="/admin">
                <i class="bx bx-wifi"></i> Portal do Admin
            </a>
            <div class="navbar-nav ms-auto">
                <a class="nav-link" href="/admin">
                    <i class="bx bx-home"></i> Painel
                </a>
                <a class="nav-link active" href="/admin/cable-network">
                    <i class="bx bx-network-chart"></i> Rede de Cabos
                </a>
                <a class="nav-link" href="/admin/cable-network/odp">
                    <i class="bx bx-broadcast"></i> Gerenciamento de ODP
                </a>
                <a class="nav-link" href="/admin/cable-network/cables">
                    <i class="bx bx-cable-car"></i> Rotas de Cabo
                </a>
                <a class="nav-link" href="/admin/cable-network/odp-connections">
                    <i class="bx bx-git-branch"></i> Conexões ODP
                </a>
                <a class="nav-link" href="/admin/billing/mapping-new">
                    <i class="bx bx-map"></i> Visualização do Mapa
                </a>
            </div>
        </div>
    </nav>

    <div class="container-fluid mt-4">
        <!-- Header -->
        <div class="row mb-4">
            <div class="col-12">
                <div class="d-flex justify-content-between align-items-center">
                    <div>
                        <h2><i class="bx bx-network-chart"></i> Painel de Rede de Cabos</h2>
                        <p class="text-muted">Visão geral da rede de cabos e infraestrutura ODP</p>
                    </div>
                    <div>
                        <a href="/admin/cable-network/odp" class="btn btn-primary me-2">
                            <i class="bx bx-plus"></i> Adicionar ODP
                        </a>
                        <a href="/admin/billing/mapping-new" class="btn btn-outline-primary">
                            <i class="bx bx-map"></i> Ver Mapa
                        </a>
                    </div>
                </div>
            </div>
        </div>

        <!-- Statistics Cards -->
        <div class="row mb-4">
            <div class="col-md-3 mb-3">
                <div class="card stat-card h-100">
                    <div class="card-body">
                        <div class="d-flex align-items-center">
                            <div class="stat-icon stat-odp me-3">
                                <i class="bx bx-broadcast"></i>
                            </div>
                            <div>
                                <h4 class="mb-0 text-primary"><%= stats.total_odps || 0 %></h4>
                                <p class="text-muted mb-0">Total de ODPs</p>
                                <small class="text-success">
                                    <i class="bx bx-check-circle"></i> 
                                    <%= stats.active_odps || 0 %> Ativos
                                </small>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
            <div class="col-md-3 mb-3">
                <div class="card stat-card h-100">
                    <div class="card-body">
                        <div class="d-flex align-items-center">
                            <div class="stat-icon stat-cable me-3">
                                <i class="bx bx-cable-car"></i>
                            </div>
                            <div>
                                <h4 class="mb-0 text-primary"><%= stats.total_cables || 0 %></h4>
                                <p class="text-muted mb-0">Total de Rotas de Cabo</p>
                                <small class="text-success">
                                    <i class="bx bx-check-circle"></i> 
                                    <%= stats.connected_cables || 0 %> Conectadas
                                </small>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
            <div class="col-md-3 mb-3">
                <div class="card stat-card h-100">
                    <div class="card-body">
                        <div class="d-flex align-items-center">
                            <div class="stat-icon stat-customer me-3">
                                <i class="bx bx-user"></i>
                            </div>
                            <div>
                                <h4 class="mb-0 text-primary"><%= stats.mapped_customers || 0 %></h4>
                                <p class="text-muted mb-0">Clientes Mapeados</p>
                                <small class="text-info">
                                    <i class="bx bx-map"></i> 
                                    Com Coordenadas
                                </small>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
            <div class="col-md-3 mb-3">
                <div class="card stat-card h-100">
                    <div class="card-body">
                        <div class="d-flex align-items-center">
                            <div class="stat-icon stat-network me-3">
                                <i class="bx bx-wrench"></i>
                            </div>
                            <div>
                                <h4 class="mb-0 text-primary"><%= stats.maintenance_odps || 0 %></h4>
                                <p class="text-muted mb-0">Em Manutenção</p>
                                <small class="text-warning">
                                    <i class="bx bx-wrench"></i> 
                                    Em Andamento
                                </small>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <!-- Quick Actions -->
        <div class="row mb-4">
            <div class="col-12">
                <h5><i class="bx bx-zap"></i> Ações Rápidas</h5>
            </div>
            <div class="col-md-3 mb-3">
                <div class="card quick-action-card h-100" onclick="window.location.href='/admin/cable-network/odp'">
                    <div class="card-body text-center">
                        <div class="stat-icon stat-odp mx-auto mb-3">
                            <i class="bx bx-broadcast"></i>
                        </div>
                        <h6>Gerenciar ODPs</h6>
                        <p class="text-muted small">Adicione, edite e gerencie Pontos de Distribuição Óptica</p>
                    </div>
                </div>
            </div>
            <div class="col-md-3 mb-3">
                <div class="card quick-action-card h-100" onclick="window.location.href='/admin/cable-network/cables'">
                    <div class="card-body text-center">
                        <div class="stat-icon stat-cable mx-auto mb-3">
                            <i class="bx bx-cable-car"></i>
                        </div>
                        <h6>Gerenciar Cabos</h6>
                        <p class="text-muted small">Configure rotas e conexões de cabos</p>
                    </div>
                </div>
            </div>
            <div class="col-md-3 mb-3">
                <div class="card quick-action-card h-100" onclick="window.location.href='/admin/cable-network/odp-connections'">
                    <div class="card-body text-center">
                        <div class="stat-icon stat-customer mx-auto mb-3">
                            <i class="bx bx-git-branch"></i>
                        </div>
                        <h6>Conexões ODP</h6>
                        <p class="text-muted small">Gerencie conexões de backbone entre ODPs</p>
                    </div>
                </div>
            </div>
            <div class="col-md-3 mb-3">
                <div class="card quick-action-card h-100" onclick="window.location.href='/admin/billing/mapping-new'">
                    <div class="card-body text-center">
                        <div class="stat-icon stat-customer mx-auto mb-3">
                            <i class="bx bx-map"></i>
                        </div>
                        <h6>Mapa da Rede</h6>
                        <p class="text-muted small">Veja o mapa interativo da rede</p>
                    </div>
                </div>
            </div>
            <div class="col-md-3 mb-3">
                <div class="card quick-action-card h-100" onclick="window.location.href='/admin/cable-network/analytics'">
                    <div class="card-body text-center">
                        <div class="stat-icon stat-network mx-auto mb-3">
                            <i class="bx bx-bar-chart-alt-2"></i>
                        </div>
                        <h6>Análises</h6>
                        <p class="text-muted small">Veja análises e relatórios da rede</p>
                    </div>
                </div>
            </div>
        </div>

        <!-- Recent Activity -->
        <div class="row">
            <div class="col-md-6">
                <div class="card h-100">
                    <div class="card-header">
                        <h6 class="mb-0"><i class="bx bx-time"></i> ODPs Recentes</h6>
                    </div>
                    <div class="card-body">
                        <% if (recentODPs && recentODPs.length > 0) { %>
                            <% recentODPs.forEach(odp => { %>
                            <div class="recent-item p-3 mb-2 rounded">
                                <div class="d-flex justify-content-between align-items-center">
                                    <div>
                                        <h6 class="mb-1"><%= odp.name %></h6>
                                        <small class="text-muted">
                                            <i class="bx bx-map-pin"></i> <%= odp.address || 'Sem endereço' %>
                                        </small>
                                    </div>
                                    <div class="text-end">
                                        <span class="badge <%= odp.status === 'active' ? 'bg-success' : odp.status === 'maintenance' ? 'bg-warning' : 'bg-secondary' %>">
                                            <%= odp.status === 'active' ? 'Ativo' : odp.status === 'maintenance' ? 'Manutenção' : odp.status %>
                                        </span>
                                        <br>
                                        <small class="text-muted">
                                            <%= new Date(odp.created_at).toLocaleDateString() %>
                                        </small>
                                    </div>
                                </div>
                            </div>
                            <% }); %>
                        <% } else { %>
                            <div class="text-center text-muted">
                                <i class="bx bx-info-circle fs-1"></i>
                                <p>Nenhum ODP encontrado</p>
                                <a href="/admin/cable-network/odp" class="btn btn-sm btn-primary">Adicionar Primeiro ODP</a>
                            </div>
                        <% } %>
                    </div>
                </div>
            </div>
            <div class="col-md-6">
                <div class="card h-100">
                    <div class="card-header">
                        <h6 class="mb-0"><i class="bx bx-cable-car"></i> Rotas de Cabo Recentes</h6>
                    </div>
                    <div class="card-body">
                        <% if (recentCables && recentCables.length > 0) { %>
                            <% recentCables.forEach(cable => { %>
                            <div class="recent-item p-3 mb-2 rounded">
                                <div class="d-flex justify-content-between align-items-center">
                                    <div>
                                        <h6 class="mb-1"><%= cable.customer_name || 'Cliente Desconhecido' %></h6>
                                        <small class="text-muted">
                                            <i class="bx bx-phone"></i> <%= cable.customer_phone || 'Sem telefone' %>
                                        </small>
                                    </div>
                                    <div class="text-end">
                                        <span class="badge <%= cable.status === 'connected' ? 'bg-success' : cable.status === 'disconnected' ? 'bg-danger' : cable.status === 'maintenance' ? 'bg-warning' : 'bg-secondary' %>">
                                            <%= cable.status === 'connected' ? 'Conectado' : cable.status === 'disconnected' ? 'Desconectado' : cable.status === 'maintenance' ? 'Manutenção' : cable.status %>
                                        </span>
                                        <br>
                                        <small class="text-muted">
                                            <%= new Date(cable.created_at).toLocaleDateString() %>
                                        </small>
                                    </div>
                                </div>
                            </div>
                            <% }); %>
                        <% } else { %>
                            <div class="text-center text-muted">
                                <i class="bx bx-info-circle fs-1"></i>
                                <p>Nenhuma rota de cabo encontrada</p>
                                <a href="/admin/cable-network/cables" class="btn btn-sm btn-primary">Adicionar Primeira Rota de Cabo</a>
                            </div>
                        <% } %>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Scripts -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
