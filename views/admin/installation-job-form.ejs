<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <title><%= job ? 'Editar Agendamento' : 'Criar Nova Instalação' %> - Painel Admin</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css" rel="stylesheet">
    <link href="/css/responsive-admin.css" rel="stylesheet">
    <link href="/css/admin-mobile.css" rel="stylesheet">
</head>
<body>
<div class="container-fluid">
    <div class="row">
        <%- include('../partials/admin-responsive-sidebar', { page: 'installations', settings: settings }) %>
        
        <main class="col-md-10 ms-sm-auto main-content">
            <div class="d-flex justify-content-between flex-wrap flex-md-nowrap align-items-center pt-3 pb-2 mb-3 border-bottom">
                <h1 class="h2">
                    <i class="bi bi-plus-circle me-2"></i><%= job ? 'Editar Agendamento' : 'Criar Nova Instalação' %>
                </h1>
                <div class="btn-toolbar mb-2 mb-md-0">
                    <div class="btn-group me-2">
                        <a href="/admin/installations" class="btn btn-outline-secondary">
                            <i class="bi bi-arrow-left me-2"></i>Voltar para a Lista
                        </a>
                    </div>
                </div>
            </div>

            <div class="row">
                <div class="col-lg-8">
                    <div class="card form-card">
                        <div class="card-body">
                            <form id="installationForm" method="POST" action="<%= job ? '/admin/installations/edit/' + job.id : '/admin/installations/create' %>">
                                <div class="form-section">
                                    <h5><i class="bi bi-person me-2"></i>Informações do Cliente</h5>
                                    
                                    <div class="row mb-3">
                                        <div class="col-md-8">
                                            <label for="customerSearch" class="form-label">Buscar Cliente</label>
                                            <div class="input-group">
                                                <input type="text" class="form-control" id="customerSearch" 
                                                       placeholder="Digite nome, telefone ou usuário" 
                                                       <%= job ? 'disabled' : '' %>>
                                                <button class="btn btn-outline-secondary" type="button" onclick="searchCustomer()" <%= job ? 'disabled' : '' %>>
                                                    <i class="bi bi-search"></i>
                                                </button>
                                            </div>
                                        </div>
                                        <div class="col-md-4">
                                            <label class="form-label">&nbsp;</label>
                                            <div class="d-grid">
                                                <button type="button" class="btn btn-outline-primary" onclick="showNewCustomerForm()" <%= job ? 'disabled' : '' %>>
                                                    <i class="bi bi-person-plus me-2"></i>Novo Cliente
                                                </button>
                                            </div>
                                        </div>
                                    </div>

                                    <div id="customerSearchResult" class="customer-search-result"></div>

                                    <div id="customerInfo" class="customer-info" style="<%= job ? 'display: block;' : '' %>">
                                        <% if (job) { %>
                                            <h6><i class="bi bi-person-check me-2"></i>Cliente Selecionado:</h6>
                                            <p><strong>Nome:</strong> <%= job.customer_name %><br>
                                               <strong>Telefone:</strong> <%= job.customer_phone %><br>
                                               <strong>Endereço:</strong> <%= job.customer_address %></p>
                                            <input type="hidden" name="customer_id" value="<%= job.customer_id %>">
                                        <% } %>
                                    </div>

                                    <div id="newCustomerForm" style="display: none;">
                                        <div class="row">
                                            <div class="col-md-6">
                                                <label for="newCustomerName" class="form-label">Nome do Cliente *</label>
                                                <input type="text" class="form-control" id="newCustomerName" name="newCustomerName">
                                            </div>
                                            <div class="col-md-6">
                                                <label for="newCustomerPhone" class="form-label">Número de Telefone *</label>
                                                <input type="tel" class="form-control" id="newCustomerPhone" name="newCustomerPhone">
                                            </div>
                                        </div>
                                        <div class="row mt-3">
                                            <div class="col-md-6">
                                                <label for="newCustomerEmail" class="form-label">E-mail</label>
                                                <input type="email" class="form-control" id="newCustomerEmail" name="newCustomerEmail">
                                            </div>
                                            <div class="col-md-6">
                                                <label for="newCustomerAddress" class="form-label">Endereço *</label>
                                                <textarea class="form-control" id="newCustomerAddress" name="newCustomerAddress" rows="2"></textarea>
                                            </div>
                                        </div>
                                    </div>
                                </div>

                                <div class="form-section">
                                    <h5><i class="bi bi-box me-2"></i>Seleção de Plano</h5>
                                    <div class="row mb-3">
                                        <div class="col-md-12">
                                            <label for="package_id" class="form-label">Plano de Internet *</label>
                                            <select class="form-select" id="package_id" name="package_id" required>
                                                <option value="">Selecione o Plano</option>
                                                <% if (Array.isArray(packages)) { %>
                                                    <% packages.forEach(p => { %>
                                                        <option value="<%= p.id %>" <%= (job && job.package_id == p.id) ? 'selected' : '' %> >
                                                            <%= p.name %> - <%= p.speed %> - R$ <%= Number(p.price).toLocaleString('pt-BR') %>
                                                        </option>
                                                    <% }) %>
                                                <% } %>
                                            </select>
                                        </div>
                                    </div>
                                </div>

                                <div class="form-section">
                                    <h5><i class="bi bi-calendar-event me-2"></i>Detalhes da Instalação</h5>
                                    <div class="row mt-3">
                                        <div class="col-md-6">
                                            <label for="assignedTechnician" class="form-label">Atribuir Técnico</label>
                                            <select class="form-select" id="assignedTechnician" name="assigned_technician_id">
                                                <option value="">Selecione o Técnico</option>
                                                <% if (typeof technicians !== 'undefined' && technicians.length > 0) { %>
                                                    <% technicians.forEach(tech => { %>
                                                        <option value="<%= tech.id %>" <%= (job && job.assigned_technician_id == tech.id) ? 'selected' : '' %> >
                                                            <%= tech.name %> (<%= tech.phone %>)
                                                        </option>
                                                    <% }); %>
                                                <% } %>
                                            </select>
                                        </div>
                                        <div class="col-md-6">
                                            <label for="priority" class="form-label">Prioridade</label>
                                            <select class="form-select" id="priority" name="priority">
                                                <option value="normal" <%= (job && job.priority == 'normal') ? 'selected' : '' %>>Normal</option>
                                                <option value="high" <%= (job && job.priority == 'high') ? 'selected' : '' %>>Alta</option>
                                                <option value="urgent" <%= (job && job.priority == 'urgent') ? 'selected' : '' %>>Urgente</option>
                                            </select>
                                        </div>
                                    </div>
                                </div>

                                <div class="form-section">
                                    <h5><i class="bi bi-info-circle me-2"></i>Informações Adicionais</h5>
                                    <div class="row">
                                        <div class="col-md-12">
                                            <label for="notes" class="form-label">Observações</label>
                                            <textarea class="form-control" id="notes" name="notes" rows="3" placeholder="Observações adicionais sobre a instalação"><%= job ? job.notes : '' %></textarea>
                                        </div>
                                    </div>
                                </div>

                                <div class="d-flex justify-content-between mt-4">
                                    <button type="button" class="btn btn-outline-secondary" onclick="resetForm()">
                                        <i class="bi bi-arrow-clockwise me-2"></i>Limpar
                                    </button>
                                    <div>
                                        <button type="submit" class="btn btn-primary">
                                            <i class="bi bi-check-circle me-2"></i><%= job ? 'Atualizar Agendamento' : 'Criar Agendamento' %>
                                        </button>
                                    </div>
                                </div>
                            </form>
                        </div>
                    </div>
                </div>
            </div>
        </main>
    </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
<script>
    // A lógica de busca de cliente e o resto do script permanecem os mesmos.
    function searchCustomer() {
        // ... 
    }
</script>
</body>
</html>
