<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> - <%= appSettings.companyHeader %></title>
    <link rel="manifest" href="/manifest.webmanifest">
    <meta name="theme-color" content="#0B1437">
    <link rel="apple-touch-icon" href="/img/icons/icon-192.png">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/admin-mobile-billing.css">
    <link href="/css/admin-mobile.css" rel="stylesheet">
    <style>
        :root {
            --primary-color: #4361ee;
            --secondary-color: #3f37c9;
            --accent-color: #4895ef;
            --success-color: #4cc9f0;
            --warning-color: #f72585;
            --info-color: #560bad;
            --light-color: #f8f9fa;
            --dark-color: #212529;
            --card-border-radius: 16px;
            --btn-border-radius: 50px;
            --transition-speed: 0.3s;
        }
        body {
            background: linear-gradient(135deg, #f8f9fa 0%, #e9ecef 100%);
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            min-height: 100vh;
            color: var(--dark-color);
            line-height: 1.6;
            padding-bottom: 80px;
        }
        .mobile-header {
            background: linear-gradient(135deg, var(--primary-color) 0%, var(--secondary-color) 100%);
            color: white;
            padding: 1rem;
            margin-bottom: 1rem;
            border-radius: 0 0 20px 20px;
            box-shadow: 0 4px 15px rgba(67, 97, 238, 0.3);
        }
        .settings-card {
            background: white;
            border-radius: 20px;
            padding: 1.5rem;
            margin-bottom: 1rem;
            box-shadow: 0 4px 20px rgba(0,0,0,0.08);
            border: 1px solid rgba(0,0,0,0.05);
        }
        .settings-section {
            margin-bottom: 2rem;
        }
        .settings-section h5 {
            color: var(--primary-color);
            margin-bottom: 1rem;
            font-weight: 600;
            display: flex;
            align-items: center;
            gap: 0.5rem;
        }
        .setting-item {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 1rem 0;
            border-bottom: 1px solid rgba(0,0,0,0.05);
        }
        .setting-item:last-child {
            border-bottom: none;
        }
        .setting-label {
            font-weight: 500;
            color: var(--dark-color);
        }
        .setting-value {
            color: #6c757d;
            font-size: 0.9rem;
        }
        .action-buttons {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 1rem;
            margin-top: 1.5rem;
        }
        .action-btn {
            padding: 0.75rem 1rem;
            border: none;
            border-radius: 12px;
            font-weight: 600;
            text-decoration: none;
            text-align: center;
            transition: all 0.3s ease;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 0.5rem;
        }
        .btn-save {
            background: linear-gradient(135deg, #28a745, #20c997);
            color: white;
        }
        .btn-save:hover {
            transform: translateY(-2px);
            box-shadow: 0 4px 15px rgba(40, 167, 69, 0.4);
            color: white;
        }
        .btn-reset {
            background: linear-gradient(135deg, #ffc107, #ff8c00);
            color: white;
        }
        .btn-reset:hover {
            transform: translateY(-2px);
            box-shadow: 0 4px 15px rgba(255, 193, 7, 0.4);
            color: white;
        }
        .form-control {
            border-radius: 10px;
            border: 1px solid #e9ecef;
            padding: 0.75rem;
            font-size: 0.9rem;
        }
        .form-control:focus {
            border-color: var(--primary-color);
            box-shadow: 0 0 0 0.2rem rgba(67, 97, 238, 0.25);
        }
        .form-label {
            font-weight: 500;
            color: var(--dark-color);
            margin-bottom: 0.5rem;
        }
        @media (max-width: 768px) {
            .mobile-header {
                padding: 1rem;
                margin-bottom: 1rem;
            }
            .settings-card {
                padding: 1rem;
                margin-bottom: 1rem;
            }
            .action-buttons {
                grid-template-columns: 1fr;
                gap: 0.75rem;
            }
        }
    </style>
</head>
<body>
    <!-- Mobile Header -->
    <div class="mobile-header">
        <div class="d-flex align-items-center justify-content-between">
            <div>
                <h4 class="mb-1">Configurações de Faturamento</h4>
                <p class="mb-0 opacity-75">Gerenciar configurações do sistema de faturamento</p>
            </div>
            <div class="text-end">
                <a href="/admin/billing/mobile" class="btn btn-outline-light btn-sm">
                    <i class="bi bi-arrow-left me-1"></i>Voltar
                </a>
            </div>
        </div>
    </div>

    <!-- Main Content -->
    <div class="container-fluid">
        <div class="row">
            <div class="col-12">
                <!-- General Settings -->
                <div class="settings-card">
                    <div class="settings-section">
                        <h5><i class="bi bi-gear"></i>Configurações Gerais</h5>
                        <div class="setting-item">
                            <div>
                                <div class="setting-label">Nome da Empresa</div>
                                <div class="setting-value"><%= appSettings.companyHeader %></div>
                            </div>
                        </div>
                        <div class="setting-item">
                            <div>
                                <div class="setting-label">Slogan da Empresa</div>
                                <div class="setting-value"><%= appSettings.company_slogan || 'Não definido' %></div>
                            </div>
                        </div>
                        <div class="setting-item">
                            <div>
                                <div class="setting-label">Site da Empresa</div>
                                <div class="setting-value"><%= appSettings.company_website || 'Não definido' %></div>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- Billing Settings -->
                <div class="settings-card">
                    <div class="settings-section">
                        <h5><i class="bi bi-receipt"></i>Configurações de Faturamento</h5>
                        <div class="setting-item">
                            <div>
                                <div class="setting-label">Notas da Fatura</div>
                                <div class="setting-value"><%= appSettings.invoice_notes || 'Não definido' %></div>
                            </div>
                        </div>
                        <div class="setting-item">
                            <div>
                                <div class="setting-label">Período de Carência para Suspensão</div>
                                <div class="setting-value"><%= appSettings.suspension_grace_period_days %> dias</div>
                            </div>
                        </div>
                        <div class="setting-item">
                            <div>
                                <div class="setting-label">Perfil de Isolamento</div>
                                <div class="setting-value"><%= appSettings.isolir_profile %></div>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- Payment Settings -->
                <div class="settings-card">
                    <div class="settings-section">
                        <h5><i class="bi bi-credit-card"></i>Configurações de Pagamento</h5>
                        <div class="setting-item">
                            <div>
                                <div class="setting-label">Nome do Banco</div>
                                <div class="setting-value"><%= appSettings.payment_bank_name || 'Não definido' %></div>
                            </div>
                        </div>
                        <div class="setting-item">
                            <div>
                                <div class="setting-label">Número da Conta</div>
                                <div class="setting-value"><%= appSettings.payment_account_number || 'Não definido' %></div>
                            </div>
                        </div>
                        <div class="setting-item">
                            <div>
                                <div class="setting-label">Titular da Conta</div>
                                <div class="setting-value"><%= appSettings.payment_account_holder || 'Não definido' %></div>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- Contact Settings -->
                <div class="settings-card">
                    <div class="settings-section">
                        <h5><i class="bi bi-telephone"></i>Contato e Informações</h5>
                        <div class="setting-item">
                            <div>
                                <div class="setting-label">Telefone</div>
                                <div class="setting-value"><%= appSettings.contact_phone || 'Não definido' %></div>
                            </div>
                        </div>
                        <div class="setting-item">
                            <div>
                                <div class="setting-label">Email</div>
                                <div class="setting-value"><%= appSettings.contact_email || 'Não definido' %></div>
                            </div>
                        </div>
                        <div class="setting-item">
                            <div>
                                <div class="setting-label">WhatsApp</div>
                                <div class="setting-value"><%= appSettings.contact_whatsapp || 'Não definido' %></div>
                            </div>
                        </div>
                        <div class="setting-item">
                            <div>
                                <div class="setting-label">Endereço</div>
                                <div class="setting-value"><%= appSettings.contact_address || 'Não definido' %></div>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- Action Buttons -->
                <div class="action-buttons">
                    <button class="action-btn btn-save" onclick="saveSettings()">
                        <i class="bi bi-check-circle me-1"></i>Salvar
                    </button>
                    <button class="action-btn btn-reset" onclick="resetSettings()">
                        <i class="bi bi-arrow-clockwise me-1"></i>Resetar
                    </button>
                </div>
            </div>
        </div>
    </div>

    <!-- Bottom Navigation -->
    <nav class="bottom-nav d-md-none">
        <a class="nav-item" href="/admin/billing/mobile/customers">
            <i class="bi bi-people"></i>
            <span>Clientes</span>
        </a>
        <a class="nav-item" href="/admin/billing/mobile/invoices">
            <i class="bi bi-receipt"></i>
            <span>Faturas</span>
        </a>
        <a class="nav-item home-button" href="/admin/billing/mobile">
            <i class="bi bi-house"></i>
            <span>Início</span>
        </a>
        <a class="nav-item" href="/admin/billing/mobile/payments">
            <i class="bi bi-credit-card"></i>
            <span>Pagar</span>
        </a>
        <a class="nav-item" href="/admin/billing/mobile/map">
            <i class="bi bi-geo-alt"></i>
            <span>Mapa</span>
        </a>
    </nav>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    <script src="/js/admin-mobile-billing.js"></script>
    <script>
        function saveSettings() {
            if (navigator.vibrate) {
                navigator.vibrate(50);
            }
            const btn = event.target;
            const originalText = btn.innerHTML;
            btn.innerHTML = '<i class="bi bi-hourglass-split me-1"></i>Salvando...';
            btn.disabled = true;
            setTimeout(() => {
                alert('Configurações salvas com sucesso!');
                btn.innerHTML = originalText;
                btn.disabled = false;
            }, 2000);
        }
        
        function resetSettings() {
            if (confirm('Tem certeza de que deseja redefinir as configurações para o padrão?')) {
                if (navigator.vibrate) {
                    navigator.vibrate(50);
                }
                const btn = event.target;
                const originalText = btn.innerHTML;
                btn.innerHTML = '<i class="bi bi-hourglass-split me-1"></i>Redefinindo...';
                btn.disabled = true;
                setTimeout(() => {
                    alert('As configurações foram redefinidas com sucesso!');
                    btn.innerHTML = originalText;
                    btn.disabled = false;
                }, 2000);
            }
        }
        
        document.addEventListener('DOMContentLoaded', function() {
            document.querySelectorAll('.action-btn').forEach(btn => {
                btn.addEventListener('touchstart', function() {
                    this.style.transform = 'scale(0.95)';
                });
                btn.addEventListener('touchend', function() {
                    this.style.transform = 'scale(1)';
                });
            });
        });
    </script>
</body>
</html>
