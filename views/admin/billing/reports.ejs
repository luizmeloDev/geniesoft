<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> - <%= appSettings.companyHeader %></title>
    <link rel="manifest" href="/manifest.webmanifest">
    <meta name="theme-color" content="#0B1437">
    <link rel="apple-touch-icon" href="/img/icons/icon-192.png">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/boxicons@2.1.4/css/boxicons.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/admin-mobile-billing.css">
    <!-- Otimizações para Admin Mobile -->
    <link href="/css/admin-mobile.css" rel="stylesheet">
    <style>
        :root {
            --primary-color: #4361ee;
            --secondary-color: #3f37c9;
            --accent-color: #4895ef;
            --success-color: #4cc9f0;
            --warning-color: #f72585;
            --info-color: #560bad;
            --light-color: #f8f9fa;
            --dark-color: #212529;
            --card-border-radius: 16px;
            --btn-border-radius: 50px;
            --transition-speed: 0.3s;
        }
        
        body {
            background: linear-gradient(135deg, #f8f9fa 0%, #e9ecef 100%);
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            min-height: 100vh;
            color: var(--dark-color);
            line-height: 1.6;
            padding-bottom: 80px;
        }
        
        .mobile-header {
            background: linear-gradient(135deg, var(--primary-color) 0%, var(--secondary-color) 100%);
            color: white;
            padding: 1rem;
            margin-bottom: 1rem;
            border-radius: 0 0 20px 20px;
            box-shadow: 0 4px 15px rgba(67, 97, 238, 0.3);
        }
        
        .report-card {
            background: white;
            border-radius: 20px;
            padding: 1.5rem;
            margin-bottom: 1rem;
            box-shadow: 0 4px 20px rgba(0,0,0,0.08);
            border: 1px solid rgba(0,0,0,0.05);
        }
        
        .stats-grid {
            display: grid;
            grid-template-columns: repeat(2, 1fr);
            gap: 1rem;
            margin-bottom: 1.5rem;
        }
        
        .stat-card {
            background: linear-gradient(135deg, var(--primary-color), var(--secondary-color));
            color: white;
            padding: 1.5rem;
            border-radius: 15px;
            text-align: center;
            box-shadow: 0 4px 15px rgba(67, 97, 238, 0.3);
        }
        
        .stat-card.success {
            background: linear-gradient(135deg, #28a745, #20c997);
        }
        
        .stat-card.warning {
            background: linear-gradient(135deg, #ffc107, #ff8c00);
        }
        
        .stat-card.info {
            background: linear-gradient(135deg, #17a2b8, #138496);
        }
        
        .stat-icon {
            font-size: 2rem;
            margin-bottom: 0.5rem;
            opacity: 0.9;
        }
        
        .stat-value {
            font-size: 1.5rem;
            font-weight: 700;
            margin-bottom: 0.25rem;
        }
        
        .stat-label {
            font-size: 0.9rem;
            opacity: 0.9;
        }
        
        .report-section {
            margin-bottom: 2rem;
        }
        
        .report-section h5 {
            color: var(--primary-color);
            margin-bottom: 1rem;
            font-weight: 600;
            display: flex;
            align-items: center;
            gap: 0.5rem;
        }
        
        .report-item {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 1rem 0;
            border-bottom: 1px solid rgba(0,0,0,0.05);
        }
        
        .report-item:last-child {
            border-bottom: none;
        }
        
        .report-label {
            font-weight: 500;
            color: var(--dark-color);
        }
        
        .report-value {
            color: #6c757d;
            font-size: 0.9rem;
        }
        
        .action-buttons {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 1rem;
            margin-top: 1.5rem;
        }
        
        .action-btn {
            padding: 0.75rem 1rem;
            border: none;
            border-radius: 12px;
            font-weight: 600;
            text-decoration: none;
            text-align: center;
            transition: all 0.3s ease;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 0.5rem;
        }
        
        .btn-export {
            background: linear-gradient(135deg, #28a745, #20c997);
            color: white;
        }
        
        .btn-export:hover {
            transform: translateY(-2px);
            box-shadow: 0 4px 15px rgba(40, 167, 69, 0.4);
            color: white;
        }
        
        .btn-refresh {
            background: linear-gradient(135deg, #17a2b8, #138496);
            color: white;
        }
        
        .btn-refresh:hover {
            transform: translateY(-2px);
            box-shadow: 0 4px 15px rgba(23, 162, 184, 0.4);
            color: white;
        }
        
        .btn-back {
            background: linear-gradient(135deg, #6c757d, #495057);
            color: white;
        }
        
        .btn-back:hover {
            transform: translateY(-2px);
            box-shadow: 0 4px 15px rgba(108, 117, 125, 0.4);
            color: white;
        }
        
        .chart-placeholder {
            height: 200px;
            background: linear-gradient(135deg, #f8f9fa, #e9ecef);
            border-radius: 15px;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            color: #6c757d;
            text-align: center;
            margin: 1rem 0;
        }
        
        .chart-placeholder i {
            font-size: 3rem;
            margin-bottom: 1rem;
            opacity: 0.5;
        }
        
        @media (max-width: 768px) {
            .mobile-header {
                padding: 1rem;
                margin-bottom: 1rem;
            }
            
            .report-card {
                padding: 1rem;
                margin-bottom: 1rem;
            }
            
            .stats-grid {
                grid-template-columns: 1fr;
                gap: 0.75rem;
            }
            
            .stat-card {
                padding: 1rem;
            }
            
            .action-buttons {
                grid-template-columns: 1fr;
                gap: 0.75rem;
            }
        }
    </style>
</head>
<body>
    <div class="container-fluid">
        <div class="row">
            <%- include('../../partials/billing-sidebar') %>
            <main class="col-md-9 ms-sm-auto col-lg-10 px-md-4 main-content py-4">
    <!-- Cabeçalho Mobile (oculto no desktop) -->
    <div class="mobile-header d-md-none">
        <div class="d-flex align-items-center justify-content-between">
            <div>
                <h4 class="mb-1">Relatórios de Faturamento</h4>
                <p class="mb-0 opacity-75">Estatísticas e análises de faturamento</p>
            </div>
            <div class="text-end">
                <a href="/admin/billing/dashboard" class="btn btn-outline-light btn-sm">
                    <i class="bi bi-arrow-left me-1"></i>Voltar
                </a>
            </div>
        </div>
    </div>

    <!-- Conteúdo Principal -->
    <div class="container-fluid">
        <div class="row">
            <div class="col-12">
                <div class="d-none d-md-flex justify-content-between flex-wrap flex-md-nowrap align-items-center pt-3 pb-2 mb-3 border-bottom">
                    <h1 class="h2"><i class="bx bx-analytics"></i> Relatórios</h1>
                </div>
                <!-- Cartões de Estatísticas -->
                <div class="stats-grid">
                    <div class="stat-card">
                        <div class="stat-icon">
                            <i class="bi bi-people"></i>
                        </div>
                        <div class="stat-value"><%= stats.totalCustomers %></div>
                        <div class="stat-label">Total de Clientes</div>
                    </div>
                    <div class="stat-card success">
                        <div class="stat-icon">
                            <i class="bi bi-receipt"></i>
                        </div>
                        <div class="stat-value"><%= stats.totalInvoices %></div>
                        <div class="stat-label">Total de Faturas</div>
                    </div>
                    <div class="stat-card warning">
                        <div class="stat-icon">
                            <i class="bi bi-currency-dollar"></i>
                        </div>
                        <div class="stat-value">R$ <%= stats.totalRevenue.toLocaleString('pt-BR') %></div>
                        <div class="stat-label">Receita Total</div>
                    </div>
                    <div class="stat-card info">
                        <div class="stat-icon">
                            <i class="bi bi-clock"></i>
                        </div>
                        <div class="stat-value"><%= stats.pendingPayments %></div>
                        <div class="stat-label">Pagamentos Pendentes</div>
                    </div>
                </div>

                <!-- Relatório de Receita -->
                <div class="report-card">
                    <div class="report-section">
                        <h5><i class="bi bi-graph-up"></i>Relatório de Receita</h5>
                        <div class="chart-placeholder">
                            <i class="bi bi-bar-chart"></i>
                            <h6>Gráfico de Receita</h6>
                            <p>O recurso de gráfico estará disponível em breve para exibir tendências de receita</p>
                        </div>
                        <div class="report-item">
                            <span class="report-label">Receita de Hoje</span>
                            <span class="report-value" id="rev-today">R$ 0</span>
                        </div>
                        <div class="report-item">
                            <span class="report-label">Receita da Semana</span>
                            <span class="report-value" id="rev-week">R$ 0</span>
                        </div>
                        <div class="report-item">
                            <span class="report-label">Receita do Mês</span>
                            <span class="report-value" id="rev-month">R$ <%= stats.totalRevenue.toLocaleString('pt-BR') %></span>
                        </div>
                    </div>
                </div>

                <!-- Relatório de Clientes -->
                <div class="report-card">
                    <div class="report-section">
                        <h5><i class="bi bi-people"></i>Relatório de Clientes</h5>
                        <div class="report-item">
                            <span class="report-label">Clientes Ativos</span>
                            <span class="report-value"><%= stats.totalCustomers %></span>
                        </div>
                        <div class="report-item">
                            <span class="report-label">Novos Clientes (Este Mês)</span>
                            <span class="report-value">0</span>
                        </div>
                        <div class="report-item">
                            <span class="report-label">Clientes Inativos</span>
                            <span class="report-value">0</span>
                        </div>
                        <div class="report-item">
                            <span class="report-label">Taxa de Retenção</span>
                            <span class="report-value">100%</span>
                        </div>
                    </div>
                </div>

                <!-- Relatório de Faturas -->
                <div class="report-card">
                    <div class="report-section">
                        <h5><i class="bi bi-receipt"></i>Relatório de Faturas</h5>
                        <div class="report-item">
                            <span class="report-label">Faturas Criadas (Este Mês)</span>
                            <span class="report-value"><%= stats.totalInvoices %></span>
                        </div>
                        <div class="report-item">
                            <span class="report-label">Faturas Pagas</span>
                            <span class="report-value">0</span>
                        </div>
                        <div class="report-item">
                            <span class="report-label">Faturas Não Pagas</span>
                            <span class="report-value"><%= stats.totalInvoices %></span>
                        </div>
                        <div class="report-item">
                            <span class="report-label">Taxa de Pagamento</span>
                            <span class="report-value">0%</span>
                        </div>
                    </div>
                </div>

                <!-- Relatório de Pagamentos -->
                <div class="report-card">
                    <div class="report-section">
                        <h5><i class="bi bi-credit-card"></i>Relatório de Pagamentos</h5>
                        <div class="report-item">
                            <span class="report-label">Pagamentos com Sucesso</span>
                            <span class="report-value">0</span>
                        </div>
                        <div class="report-item">
                            <span class="report-label">Pagamentos Pendentes</span>
                            <span class="report-value"><%= stats.pendingPayments %></span>
                        </div>
                        <div class="report-item">
                            <span class="report-label">Pagamentos Falhados</span>
                            <span class="report-value">0</span>
                        </div>
                        <div class="report-item">
                            <span class="report-label">Tempo Médio de Pagamento</span>
                            <span class="report-value">-</span>
                        </div>
                    </div>
                </div>

                <!-- Botões de Ação -->
                <div class="action-buttons">
                    <button class="action-btn btn-export" onclick="exportReport()">
                        <i class="bi bi-download me-1"></i>Exportar
                    </button>
                    <button class="action-btn btn-refresh" onclick="refreshReport()">
                        <i class="bi bi-arrow-clockwise me-1"></i>Atualizar
                    </button>
                </div>
            </div>
        </div>
    </div>
            </main>
        </div>
    </div>

    <script>
    // Busca o resumo da receita da API e preenche os valores do relatório
    (function(){
        fetch('/admin/billing/api/revenue/summary')
            .then(r=>r.json())
            .then(res=>{
                if(res && res.success && res.data){
                    const d = res.data;
                    const fmt = n => 'R$ ' + Number(n||0).toLocaleString('pt-BR');
                    const elToday = document.getElementById('rev-today');
                    const elWeek = document.getElementById('rev-week');
                    const elMonth = document.getElementById('rev-month');
                    if(elToday) elToday.textContent = fmt(d.todayRevenue);
                    if(elWeek) elWeek.textContent = fmt(d.weekRevenue);
                    if(elMonth && !<%= !!stats.totalRevenue %>) elMonth.textContent = fmt(d.monthRevenue);
                }
            }).catch(()=>{});
    })();
    </script>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    <script src="/js/admin-mobile-billing.js"></script>
    <script>
        // Funções do relatório
        function exportReport() {
            if (navigator.vibrate) {
                navigator.vibrate(50);
            }
            
            const btn = event.target;
            const originalText = btn.innerHTML;
            btn.innerHTML = '<i class="bi bi-hourglass-split me-1"></i>Exportando...';
            btn.disabled = true;
            
            setTimeout(() => {
                alert('Relatório exportado com sucesso!');
                btn.innerHTML = originalText;
                btn.disabled = false;
            }, 2000);
        }
        
        function refreshReport() {
            if (navigator.vibrate) {
                navigator.vibrate(50);
            }
            
            const btn = event.target;
            const originalText = btn.innerHTML;
            btn.innerHTML = '<i class="bi bi-hourglass-split me-1"></i>Atualizando...';
            btn.disabled = true;
            
            setTimeout(() => {
                window.location.reload();
            }, 1000);
        }
        
        document.addEventListener('DOMContentLoaded', function() {
            document.querySelectorAll('.action-btn').forEach(btn => {
                btn.addEventListener('touchstart', function() {
                    this.style.transform = 'scale(0.95)';
                });
                
                btn.addEventListener('touchend', function() {
                    this.style.transform = 'scale(1)';
                });
            });
        });
    </script>
</body>
</html>
